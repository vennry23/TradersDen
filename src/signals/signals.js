export const fetchSignals = async () => {
    // Example: Replace with actual signal fetching logic
    return new Promise(resolve => {
        setTimeout(() => {
            resolve(['Signal 1', 'Signal 2', 'Signal 3']);
        }, 1000);
    });
};
